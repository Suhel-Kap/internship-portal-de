import Layout from "../components/Layout";
import {Container, Title} from "@mantine/core";

export default function ResumeBuilder(){
    return (
        <Layout>
            <Container>
                <Title order={2}>Resume Builder</Title>
            </Container>
        </Layout>
    )
}